import '../map/Level.dart';

class LevelAction {
  static Map<String, LevelAction> ActionDefinitions = new Map();

  String ColorCode = "FFFFFF";

  static void DecodeLevelAction(dynamic Action) {
    var a = Action['Actions'];
  }
}